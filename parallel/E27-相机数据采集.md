# E27 · 相机数据采集

**阶段**：Phase 4
**预计耗时**：1-2 小时
**前置**：P4-01 内参完成（彩色图能去畸变）
**对应官方实验讲义**：`~/桌面/abot开源机器人/v01.01.01.0000/实验27_相机数据采集实验.md`

## 目标

掌握 深度相机 <!-- 2026-05-03 lsusb实测修正 --> 的 4 路常用数据流（彩色 / 深度 / IR / 点云），能写一个采集脚本批量存图，为 E29 数据集制作做准备。

## 关键问题

1. `/camera/color/image_raw` 与 `/camera/color/image_rect_color` 区别？什么时候必须用 rect？
2. `/camera/depth/image_rect_raw` 是 16UC1 单位毫米还是 32FC1 单位米？读取代码注意点？
3. 深度图怎么对齐到 RGB？需要哪个 launch 参数？(`align_depth:=true`)
4. 深度相机 <!-- 2026-05-03 lsusb实测修正 --> 的 IR pair 双目能不能自己做立体匹配？相比内置深度优劣？
5. 用 `rosbag` 录 vs 写 Python 节点存 PNG，哪种适合做数据集？

## 执行步骤

### 1. 完整 launch
```bash
roslaunch realsense2_camera rs_camera.launch \
  align_depth:=true \
  enable_pointcloud:=true \
  color_width:=1280 color_height:=720 color_fps:=30
```

### 2. 看 4 路
```bash
rqt_image_view    # 切换看 color / depth / ir1 / ir2
rosrun rviz rviz   # 加 PointCloud2 看 /camera/depth/color/points
```

### 3. 写采集脚本
```python
# 简化伪代码
import cv2, rospy, cv_bridge, message_filters
# 同步订阅 color 和 depth
# 每 1 秒存一张 PNG 到 ~/abot_ws/datasets/raw/{i:04d}.png
```

### 4. rosbag 也录一段（选做）
```bash
rosbag record /camera/color/image_raw /camera/aligned_depth_to_color/image_raw -O sample.bag
```

### 5. 测试一下"未来茶园采集场景"
- 找一盆植物当"茶树"占位
- 不同距离 / 角度 / 光照采 30 张
- 看深度图在叶子上的稀疏度（叶子边缘易丢失深度）

## 产出

- `~/abot_ws/datasets/raw/`（30+ 张配对 RGB+Depth）
- `notes/E27-数据采集.md`：
  - 4 路数据流速查
  - 深度图坑点（叶子稀疏 / 反光 / 黑色物体）
  - 采集脚本代码段

## 验收

- [ ] 能写一个稳定运行的 RGBD 采集脚本
- [ ] 关键问题 1-5 都有答案
- [ ] 知道如何对齐深度到 RGB

## 执行记录

<!-- 每次执行追加一段 -->
